# SETTINGS
# Note: Lines with '#' in front of them are ignored by the program.

# Active visualizer (soundwaves, freq_spikes, pitch_spikes)
freq_spikes

# Color scheme (RGB, fade) | RGB example for green: 0,255,0
fade

# Volume sensitivity (0 to 100)
25

# Keep window on top (true, false)
false